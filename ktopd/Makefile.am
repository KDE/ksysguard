AUTOMAKE_OPTIONS = foreign

# There will be a directory for each supported platform. I still need to
# find a mechanism to surpress the compile for all others but the build
# platform.
SUBDIRS = Linux


